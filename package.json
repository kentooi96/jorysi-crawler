{
  "name": "jorysi-crawler",
  "version": "1.0.0",
  "private": true,
  "description": "External browser crawler for JORYSI GPT — runs on GitHub Actions, pushes data to the Worker /ingest endpoint.",
  "scripts": {
    "start": "node crawl.js"
  },
  "dependencies": {
    "playwright": "^1.47.2"
  }
}
